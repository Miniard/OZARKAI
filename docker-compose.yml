# Docker Compose pour Komptal
version: '3.8'

services:
  # Base de données PostgreSQL
  postgres:
    image: postgres:16-alpine
    container_name: komptal-postgres
    environment:
      POSTGRES_DB: komptal
      POSTGRES_USER: komptal
      POSTGRES_PASSWORD: komptal_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - komptal-network

  # Ollama pour modèle local (Mistral 7B)
  ollama:
    image: ollama/ollama:latest
    container_name: komptal-ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    networks:
      - komptal-network
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    # Décommenter si pas de GPU disponible
    # command: serve

  # Application Next.js (optionnel - pour production)
  # app:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   container_name: komptal-app
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     DATABASE_URL: "postgresql://komptal:komptal_password@postgres:5432/komptal?schema=public"
  #     NEXTAUTH_URL: "http://localhost:3000"
  #     NEXTAUTH_SECRET: "your-secret-key-here"
  #     OPENAI_API_KEY: "your-openai-key"
  #     OLLAMA_URL: "http://ollama:11434"
  #   depends_on:
  #     - postgres
  #     - ollama
  #   networks:
  #     - komptal-network

volumes:
  postgres_data:
  ollama_data:

networks:
  komptal-network:
    driver: bridge

